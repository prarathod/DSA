function sumArr (arr,len ){
    if(len<1){
        return;
    }

    let temp = new Array(len-1)
    for(var i = 0;i<len;i++){
        console.log(i)
    }
}
let arr = [1,2,3,4,5];
let len = arr.length;
sumArr(arr,len)